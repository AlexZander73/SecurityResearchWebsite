# Content Notes

This directory stores editorial source notes and category-level planning files.
The published website pages live in route folders (for example `scams/<slug>/index.html`).

If you prefer a content-first workflow later, you can keep article source markdown here and manually publish to static HTML routes.
